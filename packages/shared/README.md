# packages/shared

TypeScript types generated from the DRF/OpenAPI schema (TDD §5).

Consumed by both `apps/web` and `apps/mobile` so an API contract change surfaces as a type error in both clients rather than a silent runtime bug.

Status: scaffold only — generation pipeline not yet wired up.
