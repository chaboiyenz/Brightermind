# apps/mobile

React Native + Expo (managed workflow) mobile client (TDD §4.2).

Talks to the same Django REST Framework API as `apps/web`. Consumes shared types from `packages/shared`. EAS Build/Submit integrates with GitHub Actions (TDD §7).

Status: scaffold only — no app code yet.
