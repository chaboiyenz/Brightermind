# Copy to .env.local for local development (that file is gitignored).
# Must match apps/api's CORS_ALLOWED_ORIGINS and be reachable from the browser.
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# Prototype pivot (.references/roadmap/prototype-roadmap.MD) — set to "true"
# to demo prototype pages (e.g. /screening/gad7) with a static role switcher
# and zero backend running. Leave unset/false for the real, API-wired pages.
NEXT_PUBLIC_MOCK_MODE=false
