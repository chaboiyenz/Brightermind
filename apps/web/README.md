# apps/web

React + Next.js + TypeScript + Tailwind CSS web client (TDD §4.1).

Talks to the Django REST Framework API in `apps/api`. Consumes shared types from `packages/shared`.

Status: scaffold only — no app code yet.
